<Page x:Class="JewelryStore.Pages.PageAdminUsers"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      mc:Ignorable="d"
       d:DesignHeight="450" d:DesignWidth="800"
      Title="PageAdminUsers">

    <Grid Background="#FF4E9440">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Управление пользователями" FontSize="24" HorizontalAlignment="Center" Margin="10"/>

        <DataGrid Grid.Row="1" x:Name="UsersGrid" Margin="10" AutoGenerateColumns="False" CanUserAddRows="False" BeginningEdit="UsersGrid_BeginningEdit" CellEditEnding="UsersGrid_CellEditEnding" IsReadOnly="False" SelectionMode="Single" >
            <DataGrid.Columns>
                <DataGridTextColumn Header="ID" Binding="{Binding IdUser}" Width="50" IsReadOnly="True"/>
                <DataGridTextColumn Header="Логин" Binding="{Binding Login}" Width="120"/>
                <DataGridTextColumn Header="Пароль" Binding="{Binding Password}" Width="120"/>
                <DataGridTextColumn Header="Телефон" Binding="{Binding Phone}" Width="130"/>
                <DataGridTextColumn Header="Email" Binding="{Binding Email}" Width="*"/>
                <DataGridTemplateColumn Header="Роль" Width="150">
                    <DataGridTemplateColumn.CellEditingTemplate>
                        <DataTemplate>
                            <ComboBox x:Name="RoleCombo" SelectedValuePath="IdRole" DisplayMemberPath="NameRole"
                                      ItemsSource="{Binding RelativeSource={RelativeSource AncestorType=Page}, Path=RoleList}"
                                      SelectedValue="{Binding IdRole}"/>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellEditingTemplate>
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <TextBlock Text="{Binding Role.NameRole}"/>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Center" Margin="10">
            <Button x:Name="RefreshBtn" Content="Обновить" Click="Refresh_Click" Margin="5" Padding="15,5" Height="34"/>
            <Button x:Name="SaveBtn" Content="Сохранить изменения" Click="Save_Click" Margin="5" Padding="15,5" Height="34"/>
            <Button x:Name="DeleteBtn" Content="Удалить выбранного" Click="Delete_Click" Margin="5" Padding="15,5" Height="34"/>
        </StackPanel>
    </Grid>
</Page>